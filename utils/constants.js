const VOTE_STATES = {
  VALID: "Válido",
  BLANK: "En Blanco",
  INVALID: "Anulado",
}

const VOTE_STATES_ARRAY = Object.values(VOTE_STATES)

module.exports = {
  VOTE_STATES,
  VOTE_STATES_ARRAY,
}
